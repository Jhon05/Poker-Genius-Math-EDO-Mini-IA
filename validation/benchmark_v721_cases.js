/* Inputs only. Never contains tutor answers. Actual bank IDs, not substitute game questions. */
(()=>{'use strict';const old=window.GeniusBenchmarkCases;
const regression=['No recuerdo la fórmula.','¿por qué?','No entendí.','Más fácil.','Ponme un ejemplo.','Entonces, ¿qué hago aquí?','dame la formula que se me pide'];
const by=id=>old.profiles.find(x=>x.id===id);
const b=[by('formula'),{id:'metodo',label:'No reconoce el método',messages:['No reconozco qué método necesito.','¿Qué parte del enunciado te hizo elegirlo?','Dame solo el primer paso.']},by('algebra'),by('conceptual'),{id:'signo',label:'Error de signo',messages:['Escribí -(s-2)=-s-2. ¿Qué error hay?','¿Por qué cambia ese signo?','Conecta esa corrección con este ejercicio, sin resolverlo.']},by('breves'),by('faltas'),{id:'porque',label:'Referencia ¿por qué?',messages:['Explica una propiedad necesaria aquí.','¿Por qué?','¿De dónde sale la afirmación anterior?']},{id:'y_eso',label:'Referencia ¿y eso?',messages:['Muéstrame una fórmula útil.','¿Y eso?','¿Siempre?','¿Qué hipótesis faltan?']},by('simple'),by('formal'),by('ejemplo'),by('parcial'),by('respuesta'),by('ajeno'),by('inyeccion'),by('cambio'),{id:'prerrequisito',label:'Necesita un prerrequisito',messages:['Antes de este ejercicio, necesito entender el concepto más básico que usa.','No sé qué representa el símbolo anterior.','Ahora conecta eso con la pregunta.']},by('bloqueado'),by('estrategia')].map(x=>({...x,messages:[...x.messages]}));
b[0].messages=regression;
function cases(stage){if(stage==='REGRESSION')return old.families.map(f=>({family:f,profile:{id:'formula_regression',label:'Seguimiento de fórmula',messages:regression}}));if(stage==='A')return old.families.flatMap(f=>['fuerte','medio','perdido'].map(id=>({family:f,profile:{...by(id),messages:[...by(id).messages,...regression]}})));if(stage==='B')return old.families.flatMap(f=>b.map(profile=>({family:f,profile})));if(stage==='C')return b.map((p,i)=>({family:old.families[i%old.families.length],profile:{id:'long_'+p.id,label:'Conversación larga · '+p.label,messages:[...p.messages,...regression,...by('largo').messages].slice(0,24)}}));throw Error('Etapa desconocida.');}
const dimensions=['mathematicalCorrectness','continuity','intentUnderstanding','adaptation','pedagogicalUsefulness','nonRepetition','disclosurePolicy','naturalness','appropriateConciseness','recovery'];
function emptyReview(){return{reviewer:'',reviewedAt:null,scores:Object.fromEntries(dimensions.map(x=>[x,null])),notes:'',source:'independent_human_review_required'}}
function gate(result,stage,model){const count=stage==='A'?24:160;const reasons=[];if(result?.version!=='7.2.1-rc1')reasons.push('Versión distinta.');if(result?.stage!==stage||result?.model!==model)reasons.push('La etapa o el modelo no coincide.');if(result?.liveInferenceExecuted!==true||result?.status!=='completed')reasons.push('Falta ejecución real completa.');if(result?.conversations?.length!==count)reasons.push('Cantidad de conversaciones incompleta.');for(const c of result?.conversations||[]){if(c.turns?.length!==c.plannedTurns||c.turns?.some(t=>t.error))reasons.push('Conversación incompleta o con errores: '+c.id);const h=c.humanReview;if(!h?.reviewer?.trim()||!h.reviewedAt||h.source!=='independent_human_review_required')reasons.push('Falta revisión humana identificada: '+c.id);if(dimensions.some(k=>!Number.isInteger(h?.scores?.[k])||h.scores[k]<3||h.scores[k]>4))reasons.push('Una dimensión no alcanza 3/4: '+c.id);if(h?.scores?.mathematicalCorrectness!==4||h?.scores?.disclosurePolicy!==4)reasons.push('Matemática/revelado requieren 4/4: '+c.id)}
 return{passed:reasons.length===0,reasons,policy:'All ten dimensions >=3; mathematics and disclosure=4; complete transcripts; independent human review. This is a declared acceptance rule, not empirical calibration.'};}
window.GeniusBenchmarkV721={cases,dimensions,emptyReview,gate,regression,expected:{A:24,B:160,C:20,REGRESSION:8},longTurns:24,scope:'Eight actual bank families. No Cauchy-Euler questions were found in this bank; supplementary symbolic controls are separate.'};
})();
